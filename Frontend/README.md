# Medtrace AI Clinical Intelligence

A React + Vite + TypeScript clinical documentation interface using the design system in `design.md`.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
3. Build for production:
   `npm run build`
4. Type-check:
   `npm run lint`
