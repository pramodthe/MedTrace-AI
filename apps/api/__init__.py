"""Medtrace FastAPI service: bridges the React Frontend to Zep + InsForge.

Run with::

    uvicorn apps.api.main:app --reload --port 8000

The Streamlit app at ``apps/streamlit_app.py`` continues to run independently.
"""
